{
  "name": "mneo-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Persistent memory for AI agents using git refs. Four verbs (record/list/read/forget). MCP server + Claude Code skill.",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test tests packages",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "bun run --filter '*' typecheck",
    "build": "bun run --filter='mneo' build && bun run --filter='mneo-mcp' build"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "@types/node": "25.6.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
